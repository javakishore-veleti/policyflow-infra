services:
  marketing:
    image: node:18
    container_name: nextjs-marketing
    working_dir: /app
    volumes:
      - ../../../policyflow-marketing-site:/app
    command: sh -c "npm install && npm run dev"
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
